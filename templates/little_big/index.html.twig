{% extends 'base.html.twig' %}

{% block title %}Ma Little Big Ville{% endblock %}

{% block body %}
    <div class="card" style="width: 18rem;">
        <img src="{{ app.user.picture }}" class="card-img-top" alt="Photo de profil">
        <div class="card-body">
            <h5 class="card-title">Salut {{ app.user.firstName}} {{ app.user.lastName }} !</h5>
            <p class="card-text"></p>
        </div>
    </div>
{% endblock %}
